# AOMT 使用说明

## 项目简介

AOMT（交换机自动化运维工具）是一款面向网络运维人员的桌面工具，用于集中管理 H3C Comware 和 Huawei VRP 交换机。软件提供设备管理、批量命令、交互终端、巡检、配置备份、网络检测、文件传输和抓包等功能。

软件只对当前明确支持并经过验证的品牌提供自动化功能。业务命令会按文件内容原样发送，不会自动转换为其他品牌命令。

## 版本信息

- 软件名称：AOMT 交换机自动化运维工具
- 当前版本：v1.0.0
- 作者：倚栏听雨
- 支持品牌：H3C、Huawei

## 主界面

主界面由顶部菜单、左侧功能导航、设备列表、设备作业区和连接日志组成。

- **设备管理**：添加、导入、筛选和维护设备信息。
- **设备作业**：选择目标设备，执行脚本、巡检、诊断或备份。
- **运维工具**：打开 SSH、串口、文件传输、抓包和本机网络工具。
- **配置模板**：查看和调用内置或用户添加的配置模板。
- **连接日志**：显示任务过程、成功结果、失败原因和日志文件位置。

## 设备管理

### 手动添加设备

1. 打开左侧“设备管理”。
2. 填写设备名称、品牌、IP 地址、端口、用户名和认证信息。
3. 分组和标签可按实际管理需要填写。
4. 选择主机键校验策略。
5. 点击“添加设备”。

设备名称建议使用现场唯一名称，例如 `SW1`。同一 IP 地址和端口的设备不会重复加入列表。

### Excel 导入

1. 点击“导入 Excel 文件”。
2. 选择按软件模板填写的设备文件。
3. 如果认证信息已加密，按提示输入文件密码。
4. 导入完成后，在设备列表核对名称、地址、品牌、端口和用户名。

“下载 Excel 模板”可生成标准字段模板。“加密 Excel 认证信息”用于加密表格中的用户名、密码和私钥口令等敏感字段。请妥善保管加密密码，遗失后无法恢复。

### 删除设备

- 选中设备后使用“移除选中”。
- 使用“清空列表”可移除全部设备。
- 删除和清空操作会要求二次确认。
- 在设备作业中补充的临时目标会同步到设备列表；从目标管理窗口删除时也会同步移除。

## 目标设备范围

设备作业和依赖设备信息的运维工具共用同一目标范围。

1. 从设备列表选择需要操作的设备，或按分组、标签筛选。
2. 在“目标设备”中确认当前范围。
3. 点击“补充临时目标”可输入本次运行需要的额外地址和 SSH 凭据。
4. 点击“使用设备范围”可恢复为当前设备列表或筛选结果。

临时目标在本次程序运行期间有效。CIDR 网段目标仅用于批量 Ping，不会自动加入 SSH 批量任务。

## 业务命令文件

### 统一脚本

统一脚本会对所有目标设备发送同一份命令文件。

- 每行填写一条设备命令。
- 以 `#` 开头的行作为注释，不发送到设备。
- 命令按文件内容原样发送，请根据目标品牌和系统版本编写。

### 按设备匹配

按设备匹配适合每台交换机执行不同脚本。

1. 选择“按设备匹配”模式。
2. 选择脚本目录。
3. 脚本文件名必须与设备名称完全一致，例如设备 `SW1` 对应 `SW1.txt`。
4. 执行前检查设备名称和脚本文件是否一一对应。

## 批量执行

1. 选择目标设备和业务命令文件。
2. 按需启用“执行后保存配置”或“探测二层上联口”。
3. 点击“开始执行”。
4. 在“执行前预览”中逐台核对脚本来源、命令数量、脱敏后的实际命令和风险提示。
5. 勾选“我已核对目标设备、脚本来源和实际命令”，再点击“确认执行”。
6. 在连接日志查看每台设备的连接、命令和结果。
7. 点击“结果中心”查看成功、失败、耗时、命令数量和原始输出。

结果中心支持导出 Excel、CSV 和 JSON。生产设备执行配置命令前，应先完成配置备份并确认命令影响范围。

“任务 > 批量执行历史”可查看已经开始的批量命令任务，包括操作用户、开始和结束时间、脚本来源、脚本 SHA-256 指纹、执行选项以及每台设备的结果。审计数据库保存在当前 Windows 用户的 `%LOCALAPPDATA%\AOMT\task_history.db`，不保存设备密码、私钥或完整命令输出。

## SSH 交互终端

1. 打开“工具 > SSH 交互终端”。
2. 从设备下拉列表选择已导入设备，或直接输入地址。
3. 确认端口、用户名、密码或私钥认证信息。
4. 点击“连接”。
5. 连接成功后，直接在终端区域输入命令。

终端支持文本选择、复制、粘贴并发送、清屏和保存会话。设备下拉框在有设备时仍可手动输入其他地址。

## 串口控制台

1. 打开“工具 > 串口控制台”。
2. 选择串口并设置波特率、数据位、校验位、停止位、流控、编码和行结束符。
3. 点击“打开串口”。
4. 在终端区域直接输入 Console 命令。

常用交换机参数通常为 9600、8 数据位、无校验、1 停止位、无流控，但应以设备说明为准。配置可保存后重复使用。若提示拒绝访问，请关闭占用该 COM 口的其他程序后重试。

## 一键设备巡检

1. 选择目标设备。
2. 打开“一键设备巡检”。
3. 选择适合设备品牌的巡检方案，或添加自定义只读命令。
4. 开始巡检并等待结果中心打开。

巡检会汇总版本、CPU、内存、温度、风扇、电源、接口状态和硬件告警等可解析信息，同时保留原始设备输出。不同型号不支持的指标会显示为未返回或不解析，不应据此单独判断设备故障。

## 配置备份

1. 选择目标设备。
2. 点击“配置备份”。
3. 选择保存目录并开始任务。

每台设备会建立独立目录，保存 `.cfg` 配置文件和 `.json` 元数据。元数据记录设备信息、备份时间和执行结果，便于审计与后续比较。备份完成后应打开配置文件，确认内容完整且不是分页提示或错误信息。

## 网络检测工具

### 批量 Ping

可同时使用设备列表、手工地址和 CIDR 网段作为目标。网段模式会自动排除 IPv4 网络地址和广播地址，并限制单次生成的目标数量。结果显示可达性、成功率和日志文件位置。

### 端口检测

检测目标设备指定 TCP 端口是否开放，常用于确认 SSH 22 端口或其他管理服务是否可访问。

### SSH 登录测试

只验证 TCP 连接和 SSH 认证，不执行设备命令。适合在正式批量任务前检查地址、端口和凭据。

### 路由跟踪

显示本机到目标地址经过的网络路径，用于定位路由中断、绕行或延迟位置。

### IP/MAC 终端定位

读取交换机相关表项，辅助查找某个 IP 或 MAC 地址所在的设备和接口。结果取决于设备当前 ARP、MAC 地址表和邻居信息。

### 接口综合诊断

汇总目标接口的运行状态、速率、双工、错误计数和其他可用信息，用于排查链路异常。

### IPv4/IPv6 子网计算器

输入带前缀长度的 IPv4 或 IPv6 地址，可查看网络地址、地址范围、主机数量和规范化网络等信息。

### 配置对比

选择两份配置文件后查看差异，用于核对变更前后配置或不同设备之间的配置区别。

## 文件传输服务

文件传输功能让本机作为 FTP 或 TFTP 服务端，交换机作为客户端。

1. 选择绑定的本机 IP 地址和共享目录。
2. 选择 FTP 或 TFTP，并确认端口和访问权限。
3. 启动服务。
4. 在交换机上使用对应客户端命令上传或下载文件。
5. 传输完成后停止服务。

FTP 和 TFTP 不提供加密保护，只应在可信、隔离的运维网络中临时使用。Windows 防火墙需要允许对应端口和程序访问。

## 网络抓包

抓包功能调用本机 Wireshark/TShark 与 Npcap 能力。

1. 选择抓包网卡。
2. 按需填写抓包过滤器。
3. 设置保存目录、抓包时长、最大包数和文件大小。
4. 点击“开始抓包”，也可手动停止。
5. 使用内置分析查看摘要，或点击“使用 Wireshark 打开”。

打包后的 AOMT 不包含 Wireshark。换到其他电脑使用抓包功能时，需要另行安装 Wireshark 和 Npcap。普通电脑网卡只能看到本机可见流量；抓取交换机其他端口流量时，需要在交换机上配置端口镜像。

## 常用配置模板

模板区提供 H3C 和 Huawei 常见运维场景的参考配置，包括账号、SSH、Console、SNMP、ACL、NTP、VLAN 和链路相关配置。

- 双击模板可查看内容。
- 带占位符的模板应先替换设备名称、地址、账号、密码、VLAN 和接口等参数。
- 用户可批量添加自己的文本模板，也可移除选中的用户模板。
- 模板不是跨型号通用保证，执行前必须结合设备型号、软件版本和现网规划核对。

## 日志与结果

- 连接日志实时显示当前任务过程。
- “日志”用于打开日志目录或查看已生成日志。
- “清空”只清除界面当前显示，不删除已经保存的日志文件。
- “结果中心”集中查看设备级结果、耗时、错误和原始输出，并可导出。

日志、备份、抓包和导出文件可能包含设备地址、配置和认证相关信息，应存放在受控目录并按单位安全要求保管。

## 菜单栏

- **文件**：导入或加密 Excel、下载模板、退出程序。
- **设备**：添加设备、打开 SSH/串口终端、移除或清空设备。
- **任务**：选择命令、执行批量任务、巡检、备份和查看结果。
- **工具**：打开文件传输、抓包、网络检测、诊断和配置对比工具。
- **查看**：切换功能页面、收起侧栏、恢复默认布局和管理日志。
- **帮助**：打开本使用说明或查看软件版本与作者信息。

## 使用注意事项

1. 只连接和操作已获授权的设备。
2. 生产设备执行变更前先备份，并安排回退方案。
3. 批量执行前先用少量测试设备验证命令、权限和等待时间。
4. H3C 与 Huawei 命令存在差异，命令文件和模板必须与设备品牌、型号及版本匹配。
5. 私钥、Excel 密码、设备密码、配置备份和日志均应按敏感资料管理。
6. 任务失败时先查看连接日志和结果中心，再检查网络、ACL、SSH 服务、端口、账号权限和主机键策略。
