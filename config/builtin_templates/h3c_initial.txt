# H3C Comware 7：开局基础配置
# 依赖：通过 Console 或已有管理通道登录。
system-view
sysname {{DEVICE_NAME}}
vlan {{MGMT_VLAN}}
quit
interface Vlan-interface{{MGMT_VLAN}}
ip address {{MGMT_IP}} {{MGMT_MASK}}
quit
ip route-static 0.0.0.0 0 {{DEFAULT_GATEWAY}}
clock timezone Beijing add 08:00:00
return
