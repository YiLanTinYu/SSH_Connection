# ────────────────────────────────────────────────────────────────────
# 银河麒麟 V10 (SP1-2403) — x86 Hygon C86-3G — Python 3.13 专用依赖
#
# 与 requirements.txt 的区别：
#   1. 去除 PyQt5（CLI 模式无需图形界面）
#   2. 所有版本均在 Python 3.13 + 麒麟 V10 验证可用
#   3. cryptography 升至 44.x+ 以匹配 OpenSSL 3.x（麒麟内核 5.4 默认）
#   4. netmiko 依赖链中的 textfsm / ntc-templates 也显式锁定
# ────────────────────────────────────────────────────────────────────

# ── SSH 核心 ────────────────────────────────────────────────────────
# paramiko 3.5.x 完整支持 Python 3.13，无遗留 telnetlib 依赖
paramiko>=3.5.0,<4.0

# ── 网络自动化 ──────────────────────────────────────────────────────
# netmiko 4.4.0 修复了 Python 3.12+ 的兼容性问题
# 注意：netmiko 内部仍 import telnetlib（Python 3.11 已软弃用，3.13 已移除）
# main.py / cli.py 在 import netmiko 之前已注入 telnetlib_compat 兼容层
netmiko>=4.4.0,<5.0

# netmiko 依赖：ntc-templates 解析结构化输出（TextFSM）
textfsm>=1.1.3
ntc-templates>=4.1.0

# ── 数据处理 ────────────────────────────────────────────────────────
pandas>=2.2.0,<3.0
openpyxl>=3.1.2,<4.0

# ── 加密 ────────────────────────────────────────────────────────────
# cryptography 44.x 支持 OpenSSL 3.x（麒麟 V10 默认版本）
# 低于 42.x 在 Python 3.13 下有已知 API 移除问题
cryptography>=44.0.0,<47.0

# ── 纯工具库（无 C 扩展，编译无风险）────────────────────────────────
bcrypt>=4.0.0          # paramiko 可选密钥加密
PyNaCl>=1.5.0          # paramiko Ed25519 密钥支持
